function areaParede(altura, largura) {
  const area = altura * largura; // calcula a área
  console.log(`A área da parede é ${area}m²`); // imprime a área
}

areaParede(4, 5); // 20
areaParede(3, 20); // 60